define component metadata {
  class       = "example.com/terramate-aws-ecs-cluster/v1"
  version     = "1.0.0"
  name        = "AWS Fargate for Amazon ECS (AWS FARGATE)"
  description = <<-EOF
    Create AWS Fargate for Amazon ECS on AWS with a default capacity provider strategy.
  EOF
}
