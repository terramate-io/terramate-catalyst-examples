define component metadata {
  class       = "example.com/tf-aws-vpc/v1"
  version     = "1.0.0"
  name        = "Amazon Virtual Private CLoud (AWS-VPC)"
  description = <<-EOF
    Create a VPC on AWS with public and private subnets, NAT gateway, and internet gateway.
  EOF
}
